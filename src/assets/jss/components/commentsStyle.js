const styles = theme => ({
    root: {
        width: '100%',
        height: "400px",
        backgroundColor: theme.palette.background.paper,
    },
    inline: {
        display: 'inline',
    },
})

export default styles